import React from "react";
import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <div>
      Navbar
      <NavLink to="tasks" />
    </div>
  );
};

export default Navbar;
